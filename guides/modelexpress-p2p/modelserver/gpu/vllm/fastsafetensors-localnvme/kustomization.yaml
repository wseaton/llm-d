apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Optional fastsafetensors + GDS off warm local NVMe measurement workload.
# Every node re-primes from the warm NFS source, and the GDS path depends on
# node support; use this only when you specifically want a local-NVMe number.
# Requires the `fst-model-cache` PVC from prewarm-job.yaml (applied
# standalone, as in the NFS test).
resources:
  - ../../../../../recipes/modelserver/base/single-host/default

namePrefix: fastsafetensors-localnvme-nvidia-gpu-vllm-

images:
  - name: REPLACE_MODEL_SERVER_IMAGE
    newName: ghcr.io/llm-d/llm-d-cuda
    newTag: v0.7.0

labels:
  - pairs:
      llm-d.ai/model: Llama-3.3-70B-Instruct
      llm-d.ai/guide: fastsafetensors-localnvme
      llm-d.ai/accelerator-variant: gpu
      llm-d.ai/accelerator-vendor: nvidia
    includeSelectors: true
    includeTemplates: true
    fields:
      - version: v1
        kind: ServiceAccount
        path: metadata/labels
        create: true
      - group: apps
        version: v1
        kind: Deployment
        path: metadata/labels
        create: true
      - group: apps
        version: v1
        kind: Deployment
        path: spec/selector/matchLabels
        create: true
      - group: apps
        version: v1
        kind: Deployment
        path: spec/template/metadata/labels
        create: true
      - group: monitoring.coreos.com
        version: v1
        kind: PodMonitor
        path: metadata/labels
        create: true
      - group: monitoring.coreos.com
        version: v1
        kind: PodMonitor
        path: spec/selector/matchLabels
        create: true
patches:
  - path: patch-vllm.yaml
